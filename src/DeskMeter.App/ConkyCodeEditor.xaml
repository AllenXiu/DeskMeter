<UserControl x:Class="DeskMeter.App.ConkyCodeEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#1E1E1E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ScrollViewer x:Name="LineScroll" Grid.Column="0" VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled">
            <TextBox x:Name="LineNumbers" IsReadOnly="True" BorderThickness="0" Background="#252526"
                     Foreground="#858585" FontFamily="Consolas" FontSize="12"
                     TextAlignment="Right" Padding="6,4,6,4" />
        </ScrollViewer>
        <RichTextBox x:Name="Editor" Grid.Column="1" BorderThickness="1,0,0,0" BorderBrush="#3F3F46"
                     Background="#1E1E1E" Foreground="#D4D4D4" FontFamily="Consolas" FontSize="12"
                     AcceptsTab="True" VerticalScrollBarVisibility="Visible"
                     HorizontalScrollBarVisibility="Auto" TextChanged="OnTextChanged" />
    </Grid>
</UserControl>
